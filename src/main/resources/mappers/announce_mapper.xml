<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woofnmeow.wnm_project_back.repository.AnnouncementMapper">

    <insert id="addAnnouncement" parameterType="com.woofnmeow.wnm_project_back.dto.AddAnnouncementReqDto">
        insert into
            announcement_tb
        values(0, #{title}, #{content}, #{type})
    </insert>

    <select id="getAnnouncements" resultType="java.util.List">

    </select>

</mapper>