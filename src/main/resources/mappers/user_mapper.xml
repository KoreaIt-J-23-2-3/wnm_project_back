<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woofnmeow.wnm_project_back.repository.UserMapper">

    <insert id="saveUser" parameterType="com.woofnmeow.wnm_project_back.entity.User">
        insert into
            user_tb
        values(0, 'ROLE_USER', #{oauth2Id}, #{provider}, #{name}, #{nickname}, #{email}, #{phoneNumber}, #{defaultAddressNumber}, #{defaultAddressName}, #{defaultAddressDetailName}, default)
    </insert>

    <select id="findUserByOauth2Id" resultType="com.woofnmeow.wnm_project_back.entity.User">
        select
            user_id,
            role_name,
            oauth2_id,
            provider,
            name,
            nickname,
            email,
            phone_number,
            default_address_number,
            default_address_name,
            default_address_detail_name,
            profile_url
        from
            user_tb
        where
            oauth2_id = #{oauth2Id}
    </select>

</mapper>